{
  "name": "recar-home",
  "version": "1.0.0",
  "private": true,
  "description": "RE:CAR long-term rental and lease homepage",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js"
  },
  "engines": {
    "node": ">=18"
  }
}
