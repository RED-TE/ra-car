{
  "name": "recar-home",
  "version": "1.0.0",
  "private": true,
  "description": "RE:CAR long-term rental and lease homepage",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js && node --check crew/guide/knowledge.js && node --check crew/guide/knowledge-data.js",
    "lint": "eslint crew/guide/knowledge.js crew/guide/knowledge-data.js tests playwright.config.js",
    "typecheck": "tsc -p jsconfig.knowledge.json",
    "test": "node --test tests/server-routes.test.js",
    "test:e2e": "playwright test",
    "build": "npm run lint && npm run typecheck && npm run check && npm test"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@playwright/test": "1.62.1",
    "eslint": "10.8.0",
    "globals": "17.9.0",
    "typescript": "7.0.2"
  }
}
